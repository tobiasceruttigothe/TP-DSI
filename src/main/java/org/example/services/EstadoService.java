package org.example.services;


import org.example.dtos.EstadoDTO;
import org.example.entities.Estado;
import org.example.entities.EventoSismo;
import org.example.repository.EstadoRepository;
import org.springframework.stereotype.Service;

import java.util.List;
import java.util.stream.Collectors;

@Service
class EstadoService {

    /*
    public boolean esAmbitoEventoSismico(Estado estado) {
        return "EventoSismico".equalsIgnoreCase(estado.getAmbito());
    }

    public boolean esNoRevisado(Estado estado) {
        return "no revisado".equalsIgnoreCase(estado.getNombre());
    }

     */
}

